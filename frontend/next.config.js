/** @type {import('next').NextConfig} */
     const nextConfig = {
       reactStrictMode: true,
       images: {
         domains: ['via.placeholder.com', 'imgur.com', 'www.shinhan.com.vn'],
       },
     };
     module.exports = nextConfig;